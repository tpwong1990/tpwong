#include <cs50.h>
#include <stdio.h>

int count_letters(string text);
int main(void)
{
    string s = get_string("Text: ");
}

int count_letters(string text)
{
    int i = 0;
    While (text[i] != /0)
    {
        if (text[i]>)

    }
}